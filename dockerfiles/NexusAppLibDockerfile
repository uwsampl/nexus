FROM nexus/base

ADD . /nexus/
RUN cd /nexus && make cleanall && make -j$($(nproc)-1) lib
ENV LIBRARY_PATH /nexus/build/lib:$LIBRARY_PATH
ENV LD_LIBRARY_PATH /nexus/build/lib:$LD_LIBRARY_PATH
ENV CPLUS_INCLUDE_PATH /nexus/src/nexus:/nexus/build/gen:$CPLUS_INCLUDE_PATH